# Firebase (same project as SDU Boutique website)
# Copy to .env and fill with values from Firebase Console → Project settings
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_STORAGE_BUCKET=sduksa.firebasestorage.app

# URL of the live website (for preview iframe in dashboard)
VITE_WEBSITE_URL=https://sduksa.web.app

# Optional: path to service account key JSON (for seed script only; do not commit the key)
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/serviceAccountKey.json
